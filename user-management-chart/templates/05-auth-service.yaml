apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
  - port: {{ .Values.service.auth.port }}
    targetPort: {{ .Values.service.auth.targetPort }}
    nodePort: {{ .Values.service.auth.nodePort }}
    name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-app
        image: auth-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: {{ .Values.service.auth.targetPort }}
        env:
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: JWT_SECRET
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: DB_PASSWORD
        - name: DB_HOST
          value: "postgres-db"
        - name: DB_NAME
          value: "userdb"
        - name: DB_USER
          value: "admin"
        - name: LOGIN_PAGE_TITLE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LOGIN_PAGE_TITLE
        - name: LOGIN_WELCOME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LOGIN_WELCOME
        - name: LOGIN_BUTTON
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LOGIN_BUTTON
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.service.auth.targetPort }}
          initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
          periodSeconds: {{ .Values.probes.periodSeconds }}
        readinessProbe:
          httpGet:
            path: /ready
            port: {{ .Values.service.auth.targetPort }}
          initialDelaySeconds: {{ .Values.probes.readinessInitialDelaySeconds }}
          periodSeconds: {{ .Values.probes.periodSeconds }}
        resources:
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}
